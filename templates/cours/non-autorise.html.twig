{% extends 'base.html.twig' %}

{% block body %}
    <div style="display: flex; justify-content: center; align-items: center; height: 80vh; text-align: center; flex-direction: column;">
        <h1>Accès refusé</h1>
        <p>Vous n'avez pas l'autorisation d'accéder à ce cours ou celui-ci n'existe pas.</p>
        <p>Si vous pensez qu'il s'agit d'une erreur, veuillez contacter un administrateur :</p>

        {% if admins is not empty %}
            <!-- Liste des administrateurs disponibles avec un lien mail -->
            <ul style="list-style: none; padding: 0;">
                {% for admin in admins %}
                    <li><a href="mailto:{{ admin.email }}">{{ admin.nom }} {{ admin.prenom }}</a></li>
                {% endfor %}
            </ul>
        {% else %}
            <p><em>Aucun administrateur disponible.</em></p>
        {% endif %}
    </div>
{% endblock %}
