<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Plateforme Pédagogique</title>
    <link rel="stylesheet" href="{{ asset('styles/accueil.css') }}">
</head>
<body>

<header>
    <div class="container">
        <div class="logo">UTBM</div>
        <nav>
            <ul class="nav-links">
                <li><a href="https://www.utbm.fr/">UTBM</a></li>
                <li><a href="https://outlook.office.com/mail/">Outlook</a></li>
                <li><a href="https://bibliotheque.utbm.fr/">Bibliothèque</a></li>
                <li><a href="https://ae.utbm.fr/">Association des Étudiants</a></li>
            </ul>
        </nav>
        <div class="login-button">
            <a href="{{path('app_login')}}" class="btn-login">Se connecter</a>
        </div>
        <div class="menu-toggle" id="menu-toggle">&#9776;</div>
    </div>
</header>

<main>
    <section class="banner">
        <div class="container">
            <h1>Plateforme Pédagogique</h1>
            <p>Bienvenue sur la plateforme pédagogique de l’UTBM</p>
        </div>
    </section>

    <section class="announcements">
        <div class="container">
            <h2>Annonces</h2>
            <div class="announcements-grid">
                {% for i in 1..3 %}
                    <div class="announcement">
                        <img src="{{ asset('build/images/actu.jpg') }}" alt="{{ i }}">
                        <h3>Actualité {{ i }}</h3>
                        <a href="#">En savoir plus</a>
                    </div>
                {% endfor %}
            </div>
        </div>
    </section>
</main>

<script src="{{ asset('build/scripts/accueil.js') }}"></script>
</body>
</html>
